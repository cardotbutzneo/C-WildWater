#include "../include/main.h"
#include "../include/fuites.h"
#include <string.h>
/*
AVL_Troncon* creerAVLTroncon(char* id, Troncons* troncon){
    AVL_Troncon* nv = malloc(sizeof(Troncons));
    if(!nv){
        return NULL;
    }
    nv->id = strdup(id);
*/